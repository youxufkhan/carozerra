# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# generated preview exports (regenerate via `python decode.py`)
out/

# packaging build output
packaging/dist/
packaging/build/
*.deb

# Windows build output + generated icon (regenerate via packaging/make-ico.py)
build/
dist/
packaging/carozerra.ico

# web/ assets are staged from assets/ (and CHANGELOG.md) at deploy time
# (see .github/workflows/pages.yml) and for local preview (see README) —
# not duplicated in git.
web/pioneer.png
web/clips/
web/CHANGELOG.md

# community-contributed raw disc dumps — source material for assets/clips/,
# not itself shipped (contains large installer binaries + redundant mirrors)
found_discs/

# local agent/tool state — never part of the project
.claude/

# OS
.DS_Store
